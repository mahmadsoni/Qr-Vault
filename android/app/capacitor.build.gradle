// This file is automatically generated by `npx cap update android`.
// It declares the native dependency for each installed Capacitor plugin.
// QR Vault currently uses no native plugins beyond @capacitor/core &
// @capacitor/android, so this starts empty and `cap sync` will populate
// it automatically as plugins are added.
android {
    // Placeholder — populated by the Capacitor CLI.
}

dependencies {
}
