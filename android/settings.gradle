// QR Vault — Android project settings.
// `npx cap sync android` (re)writes capacitor.settings.gradle with the
// list of installed Capacitor plugin modules; it is applied here.
include ':app'
apply from: 'capacitor.settings.gradle'
